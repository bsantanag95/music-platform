// Cuántos Caminos muestra el estante "Caminos" del perfil antes de la puerta
// a la página dedicada `/users/[username]/caminos` (mismo criterio de tope
// que `LISTS_PREVIEW_LIMIT`).
export const CAMINOS_PREVIEW_LIMIT = 10;
