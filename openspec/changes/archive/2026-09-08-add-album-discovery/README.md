# add-album-discovery

Superficie pública `/explore` centrada en álbumes: colecciones editoriales de un usuario
curador (`user_list_featured`, tabla aparte para no tocar `user_list`), explorar por década
y por género desde datos de catálogo, y rieles por reglas (mejor valorados, más reseñados)
con degradación grácil ante poca data.
